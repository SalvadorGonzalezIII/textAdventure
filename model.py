from google.appengine.ext import ndb
class questions(ndb.Model):
    """
    question = StringProperty(required=True)
    answer1 = StringProperty(required=True)
    answer2 = StringProperty(required=True)
    deathAnswer = StringProperty(required=True)
    """
